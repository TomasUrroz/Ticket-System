#include "gotoxy.h"
#include "cases.h"
#define ARCHIVOEMPLEADO "empleados.dat"

int main(){

    hidecursor(0);
    caseLogueo();

    //Pa que no rompa los huevos el return 0
    do
    {
    }
    while(1==1);
    return 0;
}
